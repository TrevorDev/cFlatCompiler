int main()
{
	int a, b;
	a = 0;
	b = 0;

	if (a > b) {		/* retrieve temp var, put in a0, invert it */
						/* beq	$zero, $a0, label 1 */
		putc('T');
						/* j label 2 */
	}
	/* label 1 */
	else if (a < b){
		putc('E');
	}
	else if (a == b) {
		putc('!');
	}
	/* label 2 */
	putc('h');

    return 1;
}